import Ellipse from '../Ellipse'

const Orbit = ({
  geometry,
  material,
  radius,
  position = [0, 0, 0],
  orbit = true,
  data,
} = {}) => {
  // const mesh = Ellipsis()
  const mesh = Ellipse(
    {

    }
  )







  return { mesh }
}

export default Orbit
